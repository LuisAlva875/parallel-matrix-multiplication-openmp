# parallel-matrix-multiplication-openmp
Matrix multiplication in C using sequential and parallel implementations with OpenMP, including performance evaluation through SpeedUp, Efficiency, and Overhead metrics.
